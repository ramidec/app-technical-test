---
phase: 02-navigation-header
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/_layout.tsx
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "User sees a header with a purple (#5E5CE6) circle showing initials 'AV', name 'Alexandra Voltec' in bold, and 'Active now' subtitle — arranged in a row"
    - "User sees a blue phone icon (Ionicons 'call', #007AFF) on the right of the header, tappable"
    - "Tapping the phone icon shows Alert.alert('Calling...', 'Alexandra Voltec')"
    - "User taps the back chevron (provided by Expo Router default) and leaves the chat screen"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "Stack navigator with custom headerTitle and headerRight"
      contains: "ChatHeaderTitle"
    - path: "app/_layout.tsx"
      provides: "Phone button in header"
      contains: "ChatHeaderRight"
  key_links:
    - from: "app/_layout.tsx Stack screenOptions"
      to: "ChatHeaderTitle component"
      via: "headerTitle prop"
      pattern: "headerTitle.*ChatHeaderTitle"
    - from: "app/_layout.tsx Stack screenOptions"
      to: "ChatHeaderRight component"
      via: "headerRight prop"
      pattern: "headerRight.*ChatHeaderRight"
---

<objective>
Replace the plain 'MFC AI Chat' title in _layout.tsx with a Figma-matched iOS-style navigation header — avatar circle with initials, contact name, subtitle on the left/center, and a tappable phone icon on the right.

Purpose: Completes the visual design of the chat screen so it looks like a real iOS messaging app. NAV-01, NAV-02, and NAV-03 are all delivered through this single layout file change.
Output: Updated app/_layout.tsx with ChatHeaderTitle and ChatHeaderRight components, wired into Stack screenOptions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigation-header/02-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/_layout.tsx (current state — entire file to be replaced):
```typescript
import { Stack } from "expo-router";

export default function RootLayout() {
  return (
    <Stack
      screenOptions={{
        title: 'MFC AI Chat',
      }}
    />
  );
}
```

From components/ChatInput.tsx (Ionicons import pattern to reuse):
```typescript
import { Ionicons } from '@expo/vector-icons';
// Usage: <Ionicons name="arrow-up" size={20} color="#FFFFFF" />
```

Established colors (Phase 1):
- #007AFF — iOS blue (sent bubbles, reuse for phone icon)
- #8E8E93 — gray (timestamps, reuse for subtitle)

Decisions from 02-CONTEXT.md (LOCKED — do not deviate):
- Avatar: 34px circle, background #5E5CE6, initials "AV", white text, 14px weight 600
- Name: "Alexandra Voltec", 16px, weight 600, color #000000
- Subtitle: "Active now", 12px, color #8E8E93
- Phone icon: Ionicons `call` (filled), size 22, color #007AFF
- Phone tap: Alert.alert('Calling...', 'Alexandra Voltec')
- Phone hitSlop: 8
- Back button: Expo Router default (no customization needed)
- Keep native Stack header (no headerShown: false)
- Claude's discretion: keep both components inline in _layout.tsx (no separate file)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ChatHeaderTitle and ChatHeaderRight components in _layout.tsx</name>
  <files>app/_layout.tsx</files>
  <action>
    Rewrite app/_layout.tsx to implement the full custom header. Keep both components inline in _layout.tsx (no separate file — simpler, all navigation config in one place).

    Structure:

    1. Imports needed:
       - `Stack` from `expo-router`
       - `View`, `Text`, `Pressable`, `Alert`, `StyleSheet` from `react-native`
       - `Ionicons` from `@expo/vector-icons`

    2. `ChatHeaderTitle` component (no props):
       - Outer View: `flexDirection: 'row'`, `alignItems: 'center'`, `gap: 8`
       - Avatar View: `width: 34`, `height: 34`, `borderRadius: 17`, `backgroundColor: '#5E5CE6'`, `alignItems: 'center'`, `justifyContent: 'center'`
       - Avatar Text: "AV", `color: '#FFFFFF'`, `fontSize: 14`, `fontWeight: '600'`
       - Text column View: `flexDirection: 'column'`
       - Name Text: "Alexandra Voltec", `fontSize: 16`, `fontWeight: '600'`, `color: '#000000'`
       - Subtitle Text: "Active now", `fontSize: 12`, `color: '#8E8E93'`

    3. `ChatHeaderRight` component (no props):
       - Pressable wrapping Ionicons
       - `hitSlop={8}`
       - `onPress`: `Alert.alert('Calling...', 'Alexandra Voltec')`
       - Ionicons: `name="call"`, `size={22}`, `color="#007AFF"`
       - Add `paddingRight: 4` on Pressable so icon doesn't clip against screen edge

    4. `RootLayout` default export:
       - Pass `headerTitle={() => <ChatHeaderTitle />}` to `screenOptions`
       - Pass `headerRight={() => <ChatHeaderRight />}` to `screenOptions`
       - Remove the old `title: 'MFC AI Chat'` prop
       - Keep Stack as-is (no `headerShown: false`)

    5. StyleSheet.create for all styles — no inline style objects.

    TypeScript note: `headerTitle` and `headerRight` props accept `() => React.ReactNode` — arrow functions returning JSX are correct. No special types needed.
  </action>
  <verify>
    <automated>cd /Users/ramirodecono/Documents/ChatTest/app-technical-test && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript compiles with zero errors. app/_layout.tsx exports RootLayout with Stack screenOptions containing headerTitle (ChatHeaderTitle) and headerRight (ChatHeaderRight). Both components defined in same file. NAV-01, NAV-02, NAV-03 satisfied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify header visuals on iOS simulator</name>
  <files>app/_layout.tsx</files>
  <action>Human verifies the rendered header against Figma and confirms phone icon interaction.</action>
  <verify>Human confirms visual match and Alert behavior as described in how-to-verify.</verify>
  <done>Header approved by user: avatar, name, subtitle, and phone icon match Figma; Alert fires on tap.</done>
  <what-built>
    Custom iOS navigation header in app/_layout.tsx:
    - Avatar circle (#5E5CE6) with "AV" initials (left)
    - "Alexandra Voltec" name bold + "Active now" subtitle (center-left)
    - Blue phone icon (right, tappable)
    - Back chevron provided by Expo Router default
  </what-built>
  <how-to-verify>
    1. Run the app: `npx expo start` in the project root, open on iOS simulator
    2. Confirm header shows: purple circle with "AV" | "Alexandra Voltec" bold | "Active now" gray subtitle
    3. Confirm phone icon (blue) appears on the right side of the header
    4. Tap the phone icon — confirm Alert appears with title "Calling..." and message "Alexandra Voltec"
    5. If a second screen exists: tap back chevron and confirm navigation back works
    6. Compare header layout against designs.png — avatar left of name, phone icon right
  </how-to-verify>
  <resume-signal>Type "approved" if header matches Figma, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Header renders avatar circle, name, subtitle, and phone icon without crashes
- Alert appears on phone icon tap
- No regressions in chat screen behavior (messages, keyboard, input)
</verification>

<success_criteria>
1. NAV-01: Header displays avatar (AV initials, purple circle), "Alexandra Voltec" name, "Active now" subtitle
2. NAV-02: Phone icon (Ionicons call, #007AFF) visible right side, triggers Alert on tap
3. NAV-03: Expo Router default back button present (chevron-left) — no customization needed, satisfied by default Stack behavior
4. TypeScript: zero compilation errors
5. No regressions: chat screen still works (messages render, send works, keyboard avoidance intact)
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-header/02-01-SUMMARY.md` following the summary template at @./.claude/get-shit-done/templates/summary.md
</output>
