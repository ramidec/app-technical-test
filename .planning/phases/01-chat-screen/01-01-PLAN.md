---
phase: 01-chat-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/MessageItem.tsx
autonomous: true
requirements:
  - BUB-01
  - BUB-02
  - BUB-03
  - BUB-04

must_haves:
  truths:
    - "Received bubbles render with #E9E9EB background, no border, 16px border radius"
    - "Sent bubbles render with #007AFF background, white text, 16px border radius"
    - "All bubbles have 10px vertical and 14px horizontal padding"
    - "Each bubble has a timestamp displayed below it in gray (#8E8E93), 12px font"
    - "Timestamp is left-aligned for received bubbles and right-aligned for sent bubbles"
    - "Bubble max-width is capped at 75% of screen width"
  artifacts:
    - path: "components/MessageItem.tsx"
      provides: "Fully styled message bubble with timestamp"
      exports: ["MessageItem (default)"]
  key_links:
    - from: "components/MessageItem.tsx"
      to: "types/message.ts"
      via: "Message interface and MessageRole enum"
      pattern: "MessageRole\\.User|MessageRole\\.Client"
---

<objective>
Restyle MessageItem.tsx so message bubbles match the Figma design exactly — correct colors, padding, border radius, max-width, and timestamp below each bubble.

Purpose: The current MessageItem has wrong colors (#fdfdfd received, #007bff sent), a border that should not exist, incorrect all-around 16px padding, no timestamps, and no width constraint. This restyle makes bubbles production-quality.
Output: components/MessageItem.tsx fully replaced with Figma-matching implementation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types the executor needs. No codebase exploration required. -->

From types/message.ts:
```typescript
export enum MessageRole {
  User = 'user',    // sent by the app user — blue bubble, right-aligned
  Client = 'client', // received from contact — gray bubble, left-aligned
}

export interface Message {
  id: string;
  role: MessageRole;
  content: string;
  createdAt: string;  // ISO string — use for timestamp display
  updatedAt: string;
}
```

Current MessageItem.tsx (to be replaced):
```typescript
// Wrong colors, wrong padding, has border, no timestamp, no max-width
const styles = StyleSheet.create({
  container: { padding: 16, borderRadius: 16, backgroundColor: '#fff', borderWidth: 1, borderColor: '#e0e0e0' },
  left: { marginRight: 'auto', backgroundColor: '#fdfdfd' },
  right: { marginLeft: 'auto', backgroundColor: '#007bff' },
  text: { fontSize: 16, fontWeight: '400' },
  textLeft: { color: '#000' },
  textRight: { color: '#fff' },
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle MessageItem with correct bubble design and timestamps</name>
  <files>components/MessageItem.tsx</files>
  <action>
Replace the entire contents of components/MessageItem.tsx with a production-quality implementation:

**Layout structure per bubble:**
```
<View>  ← outer row (aligns bubble left or right)
  <View>  ← bubble container (has background color, border radius, padding, max-width)
    <Text>  ← message content
  </View>
  <Text>  ← timestamp below bubble
</View>
```

**Exact style values (from decisions — do not deviate):**

Received bubble (MessageRole.Client):
- Background: #E9E9EB
- Text color: #1C1C1E (dark)
- Border: none (no borderWidth, no borderColor)
- Border radius: 16
- Padding: 10px vertical, 14px horizontal
- Alignment: left (marginRight: 'auto', alignItems: 'flex-start')
- Timestamp: gray (#8E8E93), 12px font, left-aligned

Sent bubble (MessageRole.User):
- Background: #007AFF (NOT #007bff — exact iOS blue)
- Text color: #FFFFFF
- Border: none
- Border radius: 16
- Padding: 10px vertical, 14px horizontal
- Alignment: right (marginLeft: 'auto', alignItems: 'flex-end')
- Timestamp: gray (#8E8E93), 12px font, right-aligned

Both bubbles:
- maxWidth: '75%'
- Bottom margin between bubbles: 8px (marginBottom on outer container)

Timestamp formatting:
```typescript
const timestamp = new Date(message.createdAt).toLocaleTimeString('en-US', {
  hour: 'numeric',
  minute: '2-digit',
});
```

No imports beyond what's needed: `React`, `StyleSheet`, `Text`, `View` from `react-native`, plus `Message` and `MessageRole` from `@/types/message`.

Do NOT add: animations, read receipts, swipe handlers, typing indicators (all v2/deferred).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "MessageItem|error" | head -20</automated>
  </verify>
  <done>
    - components/MessageItem.tsx compiles without TypeScript errors
    - Received bubbles: #E9E9EB background, no border, 16px radius, 10/14px padding, left-aligned
    - Sent bubbles: #007AFF background, white text, no border, 16px radius, 10/14px padding, right-aligned
    - Each bubble has a timestamp below it formatted as "h:mm a" (e.g., "2:32 PM")
    - Timestamp color #8E8E93, 12px, left-aligned for received, right-aligned for sent
    - Both bubble types capped at 75% screen width
  </done>
</task>

</tasks>

<verification>
Run TypeScript check: `npx tsc --noEmit`
Visually verify on iOS simulator: both bubble types render with correct colors, padding, and timestamps.
</verification>

<success_criteria>
- BUB-01: Received bubbles are #E9E9EB, no border, 16px radius
- BUB-02: Sent bubbles are #007AFF, white text, 16px radius
- BUB-03: All bubbles have 10px vertical / 14px horizontal padding
- BUB-04: Timestamp below each bubble, gray, 12px, aligned to bubble side
</success_criteria>

<output>
After completion, create `.planning/phases/01-chat-screen/01-01-SUMMARY.md`
</output>
